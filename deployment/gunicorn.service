[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=ubuntu
Group=ubuntu
WorkingDirectory=/home/ubuntu/DjangoReactFlash/backend
ExecStart=$GUNICORN_PATH --workers 3 --bind 127.0.0.1:8080 backend.wsgi:application

[Install]
WantedBy=multi-user.target
