[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=ec2-user
Group=ec2-user-group
WorkingDirectory=$HOME/website/backend
ExecStart=$GUNICORN_PATH --workers 3 --bind 127.0.0.1:8080 backend.wsgi:application

[Install]
WantedBy=multi-user.target
